import React from 'react';
import 'Pages/Homepage/menus.css';

const Homepage = props => (
  <center>
	  Welcome, this is the homepage which is work in progress, you gotta be a bit patient.
  </center>
);

export default Homepage;
